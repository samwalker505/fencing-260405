#pragma once

/* LilyGO T-Display S3 — onboard buttons: pull-up, pressed reads 0 */
#define APP_BUTTON_GO_GPIO 0   /* top: confirm / start / restart */
#define APP_BUTTON_NEXT_GPIO 14 /* bottom: next mode / pause / back */

#define APP_FENCING_GPIO 3
#define APP_LED_GPIO 1

#define APP_SPEED_TARGET_HITS 10
